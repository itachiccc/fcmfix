# 修復 FCM 無法喚醒已停止應用的問題（修復記錄）

> 本文件記錄了本次提交對 fcmfix 的修復內容、根因分析與驗證方法，
> 適用包名為上游 `com.kooritea.fcmfix`（未改動包名）。

## 1. 背景與問題現象

fcmfix 的原理：Google Play 服務（GMS）收到推送後，以有序廣播
`com.google.android.c2dm.intent.RECEIVE` 投遞給目標應用的
`com.google.firebase.iid.FirebaseInstanceIdReceiver`。Android 系統默認
「廣播不投遞給已停止的應用」（`broadcastIntentLocked` 中自動加
`FLAG_EXCLUDE_STOPPED_PACKAGES`），導致廣播被丟棄、回傳
`RESULT_CANCELLED`，GMS 據此記錄 `Failed to broadcast to stopped app`，
應用無法被喚醒。

fcmfix 的做法是在 system_server 中 hook `broadcastIntentLocked`，
對 FCM 廣播補加 `FLAG_INCLUDE_STOPPED_PACKAGES`（該標誌優先於
EXCLUDE），使系統照常投遞並啟動應用。

**故障現象**：
- 勾選的應用無法被 FCM 喚醒，FCM Diagnostics 持續顯示
  `Failed to broadcast to stopped app`；
- 最新版與舊版均無效，且問題「某一天突然出現」（與 GMS 自動更新無關）；
- 一個不帶篩選功能的同款實現可以強制拉起應用，加上篩選後全部失效——
  說明系統側機制可用，但「篩選 + 喚醒」鏈路斷了。

## 2. 測試環境

| 項目 | 值 |
| --- | --- |
| 設備 | OnePlus 15（CPH2747） |
| 系統 | ColorOS 16.1.0（Android 16，API 36） |
| Google Play 服務 | 26.32.34（versionCode 263234035） |
| Xposed 框架 | LSPosed 7847（libxposed API 102，KernelSU/Zygisk） |

## 3. 根因分析

### 3.1 直接原因：system_server 中的 hook 從未觸發

反編譯設備的 `/system/framework/services.jar` 發現，ColorOS 16 的廣播投遞鏈為：

```
ActivityManagerService.broadcastIntentLocked(22 參數)
  └─> mBroadcastController.broadcastIntentLocked(25 參數)   ← 薄包裝層（trace 包裹 + ColorOS 擴展）
       └─> broadcastIntentLockedTraced(26 參數)             ← 真正的投遞邏輯（葉子方法）
```

原實現只 hook `BroadcastController.broadcastIntentLocked`（包裝層）。
實測該 hook 在 ColorOS 16 上**不會觸發**（應有的日誌從未出現），
符合 LSPosed 文檔描述的「短方法被調用方內聯後 hook 回調不觸發」症狀。

### 3.2 GMS 側確認：GMS 沒有預檢，失敗發生在系統側

反編譯設備上的 GMS base.apk（26.32.34）確認：

- 投遞方 `DataMessageManager` 直接 `sendOrderedBroadcast(...)`，
  發送前不做 stopped 預檢；
- `DataMessageManager$BroadcastDoneReceiver` 收到 `RESULT_CANCELLED` 後
  檢查目標應用的 `ApplicationInfo.flags & FLAG_STOPPED` 歸因，輸出
  `Failed to broadcast to stopped app`；
- 結論：修復必須在系統側（或在 GMS 發送源頭）進行。

### 3.3 現代 Xposed API（libxposed 101）移植引入的時序問題

- `onSystemServerStarting` 在 `SystemServer.startBootstrapServices` 時回調，
  此時系統 Application 的 `ContextWrapper.attachBaseContext` 早已執行完；
- 原實現靠 hook `attachBaseContext` 獲取上下文、再初始化
  `isBootComplete` 與配置——在 system_server 中這個時機可能永遠等不到；
- 一旦 `context`/`isBootComplete`/`allowList` 任一環節未初始化，
  hook 直接 return 或 `targetIsAllow()` 恆為 false → 全部勾選應用失效。

### 3.4 操作層面：啟用模塊後需重啟

在 LSPosed 管理器里啟用模塊/改作用域後必須重啟才會注入 system_server。

## 4. 修改內容

### 4.1 BroadcastFix：多層掛接 + 反內聯

- 同時掛接以下方法（缺失的自動跳過並打日誌）：
  - `BroadcastController#broadcastIntentLockedTraced`（葉子方法，必定執行）
  - `BroadcastController#broadcastIntentLocked`
  - `ActivityManagerService#broadcastIntentLockedTraced`（AOSP 15 上 AMS 的實際邏輯）
  - `ActivityManagerService#broadcastIntentLocked`（Android 10～14 主路徑）
- 對上述方法及 AMS 入口方法（`broadcastIntentWithFeature`、
  `broadcastIntentWithFeatureWithCallback`、`broadcastIntent`、
  `broadcastIntentInPackage`）調用 `deoptimize()` 反內聯；
- 意圖參數/appOp 參數改為按類型與參數名自動定位（不再硬編碼索引）；
- 增加 hook 狀態（`BroadcastFix.getHookStatus()`），可通過配置重載廣播
  實時輸出到日誌，便於排查。

### 4.2 新增 GmsDeliveryFix（GMS 進程源頭兜底）

- 在 GMS 進程 hook `ContextWrapper`/`ContextImpl` 的 `sendBroadcast` 與
  `sendOrderedBroadcast` 重載，對 FCM 廣播、目標在允許列表時**在發送源頭**
  直接加 `FLAG_INCLUDE_STOPPED_PACKAGES`，不依賴系統側 hook（雙保險）；
- 需要模塊作用域包含 `com.google.android.gms`（已加入推薦作用域）；
- 注：Android 16 上 GMS 進程的類加載器無法加載 `ContextImpl`
  （一條無害日誌），實際生效的是 `ContextWrapper` 掛接。

### 4.3 system_server 上下文與初始化可靠性（XposedModule）

- 新增 `initSystemServerContext()`：通過
  `ActivityThread.currentActivityThread().getSystemContext()` 反射主動獲取
  系統上下文，失敗自動回退原有 attachBaseContext hook 路徑；
- 新增帶重試的初始化線程（AMS/UserManager 未就緒時重試，並註冊
  `ACTION_USER_UNLOCKED` 接收器兜底），初始化冪等只執行一次；
- 開機/解鎖時立即加載一次配置（避免第一條推送因配置未加載被丟棄）；
- `checkUserDeviceUnlockAndUpdateConfig()` 增加空值防護。

### 4.4 配置通道加固

- `onUpdateConfig()`：優先讀取遠程 SharedPreferences（要求 `init=true`）；
  失敗時回退 ContentProvider 直連模塊應用；
- 新增 `ConfigProvider`（authority `com.kooritea.fcmfix.provider`）：
  system_server 直接讀取模塊應用的 `config.json`，不依賴框架特性；
- `MainActivity.updateConfig()`：勾選時始終寫入本地 `config.json`，
  再嘗試同步遠程 SharedPreferences（失敗不再報致命錯誤），並廣播
  `com.kooritea.fcmfix.update.config` 通知重載；讀取配置同樣支持本地兜底；
- `BootCompletedReceiver`：開機後廣播通知已注入進程重載配置。

### 4.5 其它

- `XposedMain`：各 hook 模塊初始化異常隔離（單個失敗不影響其它）；
- `libxposed/XposedBridge`：新增 `deoptimize(Member)` 包裝；
- `AutoStartFix`：`isAllowStartService` hook 增加空值防護；
- 包名引用統一改為常量 `XposedModule.SELF_PACKAGE_NAME`；
- `AndroidManifest.xml`：聲明 `ConfigProvider`；
- `scope.list`：推薦作用域增加 `com.google.android.gms`。

## 5. 構建方法

環境：JDK 21、Android SDK（platforms;android-36）、Gradle 8.13 + AGP 8.13.0
（項目 wrapper 已配置）。命令：

```bat
gradlew.bat assembleRelease
```

產物：`app/build/outputs/apk/release/app-release-unsigned.apk`

## 6. 安裝與使用

1. 安裝模塊 APK（正常渠道或自簽名安裝）；
2. 在 LSPosed 管理器啟用模塊，作用域勾選
   **System Framework（system）** 與 **Google Play 服務**
   （miui 設備可加 電量和性能）；
3. **重啟設備**；
4. 打開 fcmfix，勾選需要被 FCM 喚醒的應用；
5. 用 FCM Diagnostics 或真實推送測試。

## 7. 驗證方法

### 7.1 實時檢查 hook 狀態與配置

```bash
adb shell am broadcast -a com.kooritea.fcmfix.update.config
adb logcat -d -v time | grep -iE "FcmFix|fcmfix"
```

期望（system_server 進程）：`BroadcastFix hooks=N [...]` 且
`[RemotePreferences]onUpdateConfig allowList size: N`。

### 7.2 模擬 FCM 廣播喚醒測試（A/B 對照，排除自啟動干擾）

先確認目標應用已停止：`adb shell dumpsys package <pkg> | grep "User 0:"`（`stopped=true`）。

正向（已勾選、已停止）：

```bash
adb shell am force-stop com.microsoft.teams
adb logcat -c
adb shell am broadcast -a com.google.android.c2dm.intent.RECEIVE -p com.microsoft.teams
adb logcat -d -v time | grep -iE "FcmFix|Enqueued broadcast|Start proc"
```

期望：`Send Forced Start Broadcast` 日誌、入隊 flag 含 `0x20`
（FLAG_INCLUDE_STOPPED_PACKAGES）、`Start proc ... for broadcast
{FirebaseInstanceIdReceiver}`、測試後 `stopped=false`。

對照（未勾選、同樣已停止，如 Telegram）：

```bash
adb shell am broadcast -a com.google.android.c2dm.intent.RECEIVE -p org.telegram.messenger
```

期望：無介入日誌、無 `0x20` 標誌、進程不啟動、`stopped` 保持 `true`——
證明喚醒效果完全由「勾選列表 + hook 注入標誌」決定，與自啟動設置無關。

## 8. 本機實測記錄（OnePlus 15 / ColorOS 16）

### 8.1 hook 與配置狀態

```
16:40:55.814 D/FcmFix(4394): BroadcastFix hooks=3 [BroadcastController#broadcastIntentLockedTraced,
                                 BroadcastController#broadcastIntentLocked,
                                 ActivityManagerService#broadcastIntentLocked]
16:40:55.814 D/FcmFix(4394): [RemotePreferences]onUpdateConfig allowList size: 4
```

### 8.2 GMS 進程模塊加載

```
16:41:31.688 I/fcmfix(23256): [fcmfix] start hook ReconnectManagerFix
16:41:31.692 I/fcmfix(23256): [fcmfix] start hook GmsDeliveryFix
（ContextImpl 掛接在 Android 16 GMS 類加載器下不可見，屬預期；ContextWrapper 掛接成功）
```

### 8.3 喚醒測試（A/B 對照，均為 stopped=true）

**正向（Microsoft Teams，已勾選）：**

```
16:45:58.825 D/FcmFix(4394): Send Forced Start Broadcast: com.microsoft.teams
16:45:58.832 I/ActivityManager(4394): Enqueued broadcast Intent { act=com.google.android.c2dm.intent.RECEIVE
                                      flg=0x400020 pkg=com.microsoft.teams }: 0
16:45:58.859 I/ActivityManager(4394): Start proc 26763:com.microsoft.teams/u0a421
                                      for broadcast {com.microsoft.teams/com.google.firebase.iid.FirebaseInstanceIdReceiver}
```

測試後 Teams：`stopped=true` → `stopped=false`。

**對照（Telegram，未勾選，同樣 stopped=true）：**

```
16:46:04.314 I/ActivityManager(4394): Enqueued broadcast Intent { act=com.google.android.c2dm.intent.RECEIVE
                                      flg=0x400000 pkg=org.telegram.messenger }: 0   ← 無 0x20 標誌
（無 Send Forced Start Broadcast 日誌，無 Start proc，測試後 stopped 保持 true）
```


---

